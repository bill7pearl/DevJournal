<%= form_with url: "/posts/#{@post.id}/comments", scope: :comments do |f| %>
	<%= f.text_area :text, placeholder: "Write something here...", rows: 5, required: true %>
	<%= f.submit "Comment" %>
<% end %>